---
import FeatureCard from './feature-card.astro';
---

<section class="features-section">
  <div class="features-section__container">
    <div class="features-section__grid">
      <FeatureCard title="Rich content integration">
        Mix form fields with text, images and other content in one rich text document.
      </FeatureCard>

      <FeatureCard title="Import/export forms">
        Save and load forms as JSON.
      </FeatureCard>

      <FeatureCard title="Dynamic form creation">
        Build forms programmatically with commands.
      </FeatureCard>

      <FeatureCard title="Extensible and customizable">
        Create your own custom extensions and form fields.
      </FeatureCard>

      <FeatureCard title="Framework agnostic">
        Built on web components to work anywhere.
      </FeatureCard>

      <FeatureCard title="Built on ProseMirror">
        Leverages the battle-tested ProseMirror foundation used by The New York Times and Atlassian.
      </FeatureCard>
    </div>
  </div>
</section>

<style is:global>
.features-section {
  max-width: var(--km-content-width);
  padding: 96px 0;
  margin: 0 auto;
  position: relative;
}

.features-section__grid {
  display: grid;
  gap: 32px;
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

@media (max-width: 1024px) {
  .features-section__grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 768px) {
  .features-section__grid {
    grid-template-columns: repeat(1, minmax(0, 1fr));
  }
}
</style>
